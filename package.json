{
  "name": "pyx-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "npm run build:trainer-auth",
    "build:trainer-auth": "python3 scripts/build_trainer_auth.py",
    "deploy": "bash scripts/deploy.sh all",
    "deploy:api": "bash scripts/deploy.sh api",
    "deploy:hosting": "bash scripts/deploy.sh hosting"
  }
}
