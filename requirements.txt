# Pyx AI
# Core: Python standard library only (json, math, random, pathlib, typing)
# Optional Firestore sync (pyx-ai project):
firebase-admin>=6.0.0
# Web API (app.py / gunicorn for pyxaiapi):
flask>=3.0.0
gunicorn>=21.0.0
numpy>=1.24.3,<2.0.0
# Optional: server-side traffic-light image fetch (Dev Workshop falls back to browser canvas)
Pillow>=10.0.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0+cpu; platform_system=="Linux"
torchaudio==2.11.0+cpu; platform_system=="Linux"
TTS==0.22.0
