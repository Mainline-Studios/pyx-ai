# Pyx AI
__pycache__/
*.py[cod]
*$py.class
data/
*.json
.env
.venv/
venv/
firebase-key.json

# Trainer gate — passphrase and generated HTML must not be committed
TRAINER_GATE_PASSWORD.txt
public/pyx-trainer-gate.html

# Repo config (do not ignore)
!package.json
!firebase.json
