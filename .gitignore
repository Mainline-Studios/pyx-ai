# Pyx AI
__pycache__/
*.py[cod]
*$py.class
data/
*.json
.env
.venv/
venv/
firebase-key.json

# Local secrets / tooling noise
TRAINER_GATE_PASSWORD.txt
public/pyx-trainer-auth.html
.firebase/
firebase-debug.log
.DS_Store

# Repo config (do not ignore)
!package.json
!firebase.json
!public/downloads/manifest.json

# Release CI stages installers here before Firebase deploy (large binaries — do not commit)
public/downloads/*.dmg
public/downloads/*.pkg
public/downloads/*-setup.exe
