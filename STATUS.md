# Firebase MCP — status

## Authentication

- [ ] Logged in with Firebase CLI (`firebase login`)
- [ ] Default project set for this repo (`firebase use <project-id>`)

**Last verified:** _YYYY-MM-DD_

**Account / project (optional):** _e.g. you@email.com / my-project-id_

## If MCP or CLI calls fail

1. Run `firebase login` and complete the browser flow.
2. From this repo root, run `firebase use` and pick the correct project (or `firebase use --add` if needed).
3. Restart Cursor or reload the Firebase MCP server if tools still error.

## Notes

_Add anything specific to this workspace (service account, CI, emulator ports, etc.)._
