# Trainer lock — password only (no email, no Firebase Auth UI).

# Copy to TRAINER_GATE_PASSWORD.txt (gitignored).
# Put the password on the FIRST non-empty line that does NOT start with "#".
# All other lines must be comments (start with "#") or blank.
#
# Example password line (replace with your own):
#   MyTrainerSecret-ChangeThis-2026

# Before deploy, either:
#   export PYX_TRAINER_PASSWORD='your-password'
#   python3 scripts/build_trainer_auth.py
# or put the same line in TRAINER_GATE_PASSWORD.txt and run firebase deploy (prebuild runs the script).
