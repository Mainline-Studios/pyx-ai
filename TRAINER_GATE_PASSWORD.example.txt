# Copy to TRAINER_GATE_PASSWORD.txt (gitignored). File format: ONE line only — your passphrase.
# Lines starting with # are ignored.
#
# Example passphrase line (do not use literally):
#   MyVeryLongRandomPassphrase-ChangeMe-2026
#
# Then run before deploy:
#   python3 scripts/set_gate_hash.py
#
# Or set env (CI / local shell, not committed):
#   export PYX_TRAINER_GATE_PASSWORD='your-passphrase'
#   python3 scripts/set_gate_hash.py
#
# Firebase Hosting runs this automatically via firebase.json predeploy.
#
# Rotate: change passphrase, rerun set_gate_hash.py, redeploy.
